---
const { kicker, title, description } = Astro.props;
---

<section class="statement" id="projects">
  <p class="kicker">{kicker}</p>
  <h2>{title} <em>自动发布。</em></h2>
  <p>{description}</p>
</section>
